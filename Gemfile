# This Gemfile is ONLY for running the test suite.
# The check_http_json.rb script itself has NO gem dependencies
# and can be run standalone with just Ruby's standard library.

source 'https://rubygems.org'

gem 'json'
gem 'minitest'
gem 'webmock'
gem 'rake'
